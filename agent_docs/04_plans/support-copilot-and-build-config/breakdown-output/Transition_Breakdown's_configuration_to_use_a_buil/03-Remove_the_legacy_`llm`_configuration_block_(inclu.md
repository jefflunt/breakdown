# Remove the legacy `llm` configuration block (including `provider`, `model`, and `api_key`) from `config.Config` in `pkg/config/config.go` and clean up all associated references, CLI flags, and initialization logic across the codebase.

The task involves modifying the `config.Config` struct in `pkg/config/config.go` to completely remove the old `llm` configuration block, which consists of the `provider`, `model`, and `api_key` fields. Following this structural change, the entire codebase must be cleaned up to remove any references to these fields. This involves updating the configuration parsing logic, removing legacy CLI flags or environment variable bindings (likely located in `cmd/breakdown/main.go`), and updating the `pkg/llm/factory.go` or other consumer packages to ensure they exclusively rely on the newly implemented `agent_adapter` string. Any fallback logic dealing with the old API keys and provider fields must be stripped out.
